services:
  gamemaster:
    build:
      context: ..
      dockerfile: build/unit_test/Dockerfile

    networks:
      - aimessager
    ports:
      - '8080:8080'
      - '2222:2222'

networks:
  aimessager:
