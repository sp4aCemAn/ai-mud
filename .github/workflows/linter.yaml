
name: Go Linter

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  golangci-lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      # Step 1: Check out your code
      - name: Check out code
        uses: actions/checkout@v4

      # Step 2: Set up the Go environment
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.1' 

      # Step 3: Run the golangci-lint action
      # This action will automatically find and use your .golangci.yml file.
      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v8
        with:
          # Use a specific version for reproducible builds
          version: v2.3.1
